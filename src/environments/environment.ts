// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false
  // firebaseConfig: {
    // apiKey: 'AIzaSyDfmijBqqKut1sGRedkL_3GE7UkyaOoeHo',
    // authDomain: 'my-project-d612f.firebaseapp.com',
    // databaseURL: 'https://my-project-d612f.firebaseio.com',
    // projectId: 'my-project-d612f',
    // storageBucket: 'my-project-d612f.appspot.com',
    // messagingSenderId: '231055912555',
    // appId: '1:231055912555:web:0e960d083622ff2145cb2e'
  // }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
